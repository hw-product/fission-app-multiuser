- content_for(:delete) do
  %span.glyphicon.glyphicon-trash
- content_for(:heading_title) do
  = "Edit Product - #{@product.name}"
  = link_to(yield(:delete), admin_product_path(@product.id), :class => 'btn btn-xs btn-danger pull-right', :method => :delete, :remote => true)
  .clearfix

:ruby
  id = 'edit-product'
  content_for(:container) do
    render(
      :partial => 'layouts/panel',
      :locals => {
        :id => id,
        :heading => yield(:heading_title),
        :panel_content => render(
          :partial => 'form',
          :locals => {
            :form_opts => {
              :url => admin_product_path(@product.id),
              :method => :put
            }
          }
        )
      }
    )
  end

= render(:partial => 'layouts/container', :locals => {:container_class => "#{id}-form", :container_id => id})
